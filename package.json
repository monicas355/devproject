{
  "name": "project",
  "version": "1.0.0",
  "description": "Sample project for Dockerized Node.js app",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "mocha tests/*.spec.js"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "^10.0.0"
  }
}
